<div class="container">
    <div class='session-box'>
        <% if @errors.present? %>
            <% @errors.each do |e|%>
                <p style="color: red"><%= e %></p>
            <% end %>
        <%end%>
        <%= form_for @user, url: users_path, method: :post do |f| %>
            <%= render 'form_user_info', f: f %>
            <%= f.submit '註冊', class: 'btn btn-primary', style: 'margin-left: 45%; margin-right: 45%;'%>               
        <% end %>
    </div>
</div>